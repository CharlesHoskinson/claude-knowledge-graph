[project]
name = "graphify-wiki-suite"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["PyYAML>=6", "jsonschema>=4"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
markers = ["network: tests that require network access (skipped offline)"]
