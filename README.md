# graphify-wiki-suite

Claude Code plugin bundling Graphify tooling, the `llm-wiki` skill, and
`compiling-graphify-wiki` — which compiles a Graphify knowledge-graph export into
an `llm-wiki`-format markdown wiki. See `docs` for the design spec and plan.
